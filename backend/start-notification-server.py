# Change to the NotificationServer directory
import os
os.chdir("tools/NotificationServer")

# Run the NotificationServer.py script
os.system("python3 NotificationServer.py")
        tools_dir = os.path.abspath("../..")  # Get absolute path of 'tools' directory
